<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>io.instanto</groupId>
    <artifactId>webapp-testkit</artifactId>
    <version>0.1.0-SNAPSHOT</version>
  </parent>

  <artifactId>webapp-testkit-dom</artifactId>
  <name>Webapp Testkit DOM</name>
  <description>Accessible DOM queries, interactions, waiting, geometry and a controllable clock.</description>

  <dependencies>
    <!-- Rectangle relations and tolerances, with no DOM of their own. -->
    <dependency>
      <groupId>io.instanto</groupId>
      <artifactId>webapp-testkit-layout</artifactId>
    </dependency>
    <!-- Compiler-side support for DOM casts across window boundaries. -->
    <dependency>
      <groupId>org.teavm</groupId>
      <artifactId>teavm-core</artifactId>
      <scope>provided</scope>
    </dependency>
    <dependency>
      <groupId>org.teavm</groupId>
      <artifactId>teavm-jso-apis</artifactId>
      <version>${teavm.version}</version>
    </dependency>
    <!-- DomTest is a base class and DomRule is a rule used by consumer tests. -->
    <dependency>
      <groupId>junit</groupId>
      <artifactId>junit</artifactId>
      <scope>provided</scope>
    </dependency>
    <!-- Declared before teavm-junit so rules run in this module's own tests. -->
    <dependency>
      <groupId>io.instanto</groupId>
      <artifactId>teavm-rule-support</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.teavm</groupId>
      <artifactId>teavm-classlib</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.teavm</groupId>
      <artifactId>teavm-junit</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-plugin</artifactId>
        <configuration>
          <systemPropertyVariables>
            <teavm.junit.target>${project.build.directory}/js-tests</teavm.junit.target>
            <teavm.junit.js.runner>${webapp.testkit.test.browser}</teavm.junit.js.runner>
          </systemPropertyVariables>
        </configuration>
      </plugin>
    </plugins>
  </build>
</project>
